const merge = require('./merge')
const zescape = require('./zescape')
const replace = require('./replace')
const noop = require('./noop')
const sortRules = require('./sort-rules')

module.exports = {
    merge,
    zescape,
    replace,
    noop,
    sortRules
}
